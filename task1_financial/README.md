# Task 1: Financial Sentiment & Trading Signal Pipeline

This module implements a financial analysis pipeline integrating market data, news analysis, technical indicators, and LLM-powered sentiment analysis to generate trading recommendations.

## Setup Instructions

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Run the pipeline:
   ```bash
   python -m src.pipeline
   ```
